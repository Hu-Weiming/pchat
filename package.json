{
  "name": "pchat",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@11.19.0",
  "scripts": {
    "build": "pnpm -r --if-present build",
    "check": "pnpm -r --if-present check",
    "check:tests": "tsc --noEmit -p tsconfig.tests.json",
    "check:deps": "node scripts/check-core-dependencies.mjs",
    "check:portable": "node scripts/check-portable-harness.mjs",
    "check:p1": "pnpm check && pnpm check:tests && pnpm test:p1 && pnpm check:deps && pnpm check:portable",
    "test:p1": "vitest run --configLoader runner packages/contracts packages/harness packages/testing scripts",
    "test": "vitest run --configLoader runner",
    "p0:runtime:build": "pnpm --filter @pchat/runtime-windows build",
    "p0:runtime:prepare-sidecar": "node scripts/prepare-runtime-sidecar.mjs",
    "p0:desktop:dev": "node scripts/build-desktop.mjs dev",
    "p0:desktop:build": "node scripts/build-desktop.mjs build",
    "desktop:stage": "node scripts/build-desktop.mjs stage"
  },
  "devDependencies": {
    "@types/node": "24.10.1",
    "esbuild": "0.28.2",
    "typescript": "7.0.2",
    "vite": "8.2.2",
    "vitest": "5.0.0"
  }
}
