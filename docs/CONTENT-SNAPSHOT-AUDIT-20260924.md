# 2026-09-24 已保存检索依据抽样清点

本次只读取此前本机13个人物各一条问题的千帆检索结果，不重新请求供应商，也不改写历史回答。主快照位于仓库外的`D:/Dev/temp/pchat/workflow-live-rSgPAg`；费希特和维特根斯坦使用后来修复后的`workflow-live-suH8t1`、`workflow-live-4uVUz9`。这些结果跨不同时间取得，只适合发现资料风险，不代表同一版本的全库评测。

| 观察项 | 抽样结果 | 影响 |
| --- | ---: | --- |
| 有检索依据的人物 | 13/13 | 证明这批问题有返回片段，不证明回答正确。 |
| 返回的依据片段 | 36 | 每人一条问题的抽样，不能外推为资料库覆盖率。 |
| 缺结构化版本、译者、稳定定位 | 各36/36 | 不能据此生成可核验逐字引文；当前只允许概述。文件名中的译者字样不能自动当作已确认元数据。 |
| 含文件名包装的旧片段 | 34/36 | 新检索会去掉包装并保留原始摘录区间；旧历史快照不改写。 |
| 混入独立OCR目录行 | 1 | 新检索会排除这类混合片段。 |
| 混合文集中的人物署名发言 | 1 | 缺独立访谈来源时，新检索暂不作为已确认访谈依据。 |

产品负责人后续需要核对每份资料的原典归属、思想阶段、作品或访谈名称、版本与译者，并提供可稳定复核的章节/节号或访谈定位。访谈宜单独成文档，名称明确标明访谈，片段保留人物署名；采访者和编者文字不能归于人物。当前工作流检索适配器仍把版本、译者和定位设为空，只有上传资料并不足以自动填充这些字段；在可信元数据接入和内容复核完成前，不把本抽样记为内容验收通过。
